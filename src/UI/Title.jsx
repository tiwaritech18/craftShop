

const Title = ({text}) => {
  return (
    <div className="title-container">
      <h3 className="bar">____________</h3>
      <span><h3 className="letter-spacing">{text}</h3></span>
    </div>

  )
}

export default Title
